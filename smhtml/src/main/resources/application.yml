server:
  servlet:
    context-path: /
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/database?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&useLegacyDatetimeCode=false
    username: zwl
    password: x5
  mvc:
    static-path-pattern: /public/**  # 所有页面的前缀路径